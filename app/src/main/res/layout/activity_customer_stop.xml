<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
              android:id="@+id/screen"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/appTextColorWithBackground"
              android:orientation="vertical"
              android:padding="@dimen/screen_padding"
              tools:context="br.com.whitemartins.obc.views.CustomerStopActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:text="@string/client_number"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/edtCdCliente"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:ems="10"

            android:inputType="number"
            android:maxLength="8"/>

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingTop="20dp">

        <ImageButton
            android:id="@+id/btnConfimarCliente"
            android:layout_width="@dimen/button_w"
            android:layout_height="@dimen/button_h"
            android:layout_gravity="center_vertical"
            android:background="#fafafa"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/ic_confirmar"/>

        <ImageButton
            android:id="@+id/btnListarCliente"
            android:layout_width="@dimen/button_w"
            android:layout_height="@dimen/button_h"
            android:layout_gravity="center_vertical|end"
            android:layout_marginLeft="10dp"
            android:background="#fafafa"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/ic_listar"/>
    </LinearLayout>

</LinearLayout>
